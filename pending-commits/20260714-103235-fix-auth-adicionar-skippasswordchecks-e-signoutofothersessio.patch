From: pending-queue <queue@avmd.local>
Date: Tue, 14 Jul 2026 10:32:35 -0300
Subject: fix(auth): adicionar skipPasswordChecks e signOutOfOtherSessions na alteracao de senha admin
Patch-Hash: sha256:fd3fb367791747188579e8cb070c15a92e558ffabd15084dc0e5f0214b4b6642
---
diff --git a/backend/src/routes/adminUsersRoutes.ts b/backend/src/routes/adminUsersRoutes.ts
index 22be9eb..0432f30 100644
--- a/backend/src/routes/adminUsersRoutes.ts
+++ b/backend/src/routes/adminUsersRoutes.ts
@@ -96,7 +96,11 @@ export async function handleAdminUsersRoutes(
   if (body.action === 'update_password') {
     const { userId, password } = body.payload
     try {
-      await clerkClient.users.updateUser(userId, { password })
+      await clerkClient.users.updateUser(userId, {
+        password,
+        skipPasswordChecks: true,
+        signOutOfOtherSessions: true,
+      })
       writeJson(res, 200, { ok: true }, corsOrigin)
     } catch (error) {
       writeJson(res, 400, { ok: false, error: getClerkErrorMessage(error) }, corsOrigin)
diff --git a/pending-commits/20260714-094729-fix-chat-bloquear-ia-quando-atendimento-humano-true.patch b/pending-commits/20260714-094729-fix-chat-bloquear-ia-quando-atendimento-humano-true.patch
deleted file mode 100644
index 38243be..0000000
--- a/pending-commits/20260714-094729-fix-chat-bloquear-ia-quando-atendimento-humano-true.patch
+++ /dev/null
@@ -1,127 +0,0 @@
-From: pending-queue <queue@avmd.local>
-Date: Tue, 14 Jul 2026 09:47:29 -0300
-Subject: fix(chat): bloquear IA quando atendimento_humano=true
-Patch-Hash: sha256:0c7c2d6c8c0178d4180cd79b1ab3edade5c781e27ce928a40f6891cf2ba536a9
----
-diff --git a/backend/src/routes/evolutionWebhookRoutes.ts b/backend/src/routes/evolutionWebhookRoutes.ts
-index 68089fd..1a86df0 100644
---- a/backend/src/routes/evolutionWebhookRoutes.ts
-+++ b/backend/src/routes/evolutionWebhookRoutes.ts
-@@ -1,5 +1,6 @@
- import type { IncomingMessage, ServerResponse } from 'node:http'
- import type { BackendConfig } from '../config/env.js'
-+import type { AivenSqlClient } from '../db/aivenClient.js'
- import type { CommunicationEventRepository } from '../repositories/communicationEventRepository.js'
- import type { LeadRepository } from '../repositories/leadRepository.js'
- import { readJson, writeJson } from '../utils/http.js'
-@@ -251,9 +252,22 @@ async function configureEvolutionWebhook(input: EvolutionControlInput) {
-   }
- }
- 
--async function forwardInboundToN8n(config: BackendConfig, event: NormalizedEvolutionEvent, leadId: string | null) {
-+async function forwardInboundToN8n(config: BackendConfig, event: NormalizedEvolutionEvent, leadId: string | null, db?: AivenSqlClient) {
-   if (!config.n8nWebhookUrl || event.fromMe) return { forwarded: false, error: null as string | null }
- 
-+  if (db && event.conversationId) {
-+    try {
-+      const humanCheck = await db.query<{ atendimento_humano: boolean }>(
-+        `SELECT atendimento_humano FROM crm_chat_conversations WHERE id::text = $1 OR document_key = $2 LIMIT 1`,
-+        [event.conversationId, event.contactDigits ?? ''],
-+      )
-+      const row = humanCheck.rows[0]
-+      if (row?.atendimento_humano) {
-+        return { forwarded: false, error: null, blocked_human: true }
-+      }
-+    } catch {}
-+  }
-+
-   const canal = inferCanalFromInstance(event.instanceName)
- 
-   try {
-@@ -315,6 +329,7 @@ export async function handleEvolutionWebhookRoutes(
-   communicationEventRepository: CommunicationEventRepository,
-   config: BackendConfig,
-   corsOrigin: string,
-+  db?: AivenSqlClient,
- ): Promise<boolean> {
-   const url = req.url ?? ''
-   const method = req.method ?? ''
-@@ -371,7 +386,7 @@ export async function handleEvolutionWebhookRoutes(
-       payload,
-     })
- 
--    const forwarded = await forwardInboundToN8n(config, normalized, lead?.id ?? null)
-+    const forwarded = await forwardInboundToN8n(config, normalized, lead?.id ?? null, db)
- 
-     writeJson(res, 200, {
-       ok: true,
-diff --git a/backend/src/server.ts b/backend/src/server.ts
-index cd35936..6119483 100644
---- a/backend/src/server.ts
-+++ b/backend/src/server.ts
-@@ -139,6 +139,7 @@ const server = createServer(async (req, res) => {
-       communicationEventRepository,
-       config,
-       config.corsOrigin,
-+      db,
-     )
-     if (handledEvolutionWebhook) return
- 
-diff --git a/pending-commits/20260714-092419-fix-chat-habilitar-alteracao-de-fila-email-agendamento-e-mel.patch b/pending-commits/20260714-092419-fix-chat-habilitar-alteracao-de-fila-email-agendamento-e-mel.patch
-deleted file mode 100644
-index fa53b26..0000000
---- a/pending-commits/20260714-092419-fix-chat-habilitar-alteracao-de-fila-email-agendamento-e-mel.patch
-+++ /dev/null
-@@ -1,52 +0,0 @@
--From: pending-queue <queue@avmd.local>
--Date: Tue, 14 Jul 2026 09:24:19 -0300
--Subject: fix(chat): habilitar alteracao de fila (email/agendamento) e melhorar resumo
--Patch-Hash: sha256:644250faf1c431fc576fb1a86cc21c15e0173fc07fb73dd816af193e7f534cab
-----
--diff --git a/src/pages/ChatInboxCRM.tsx b/src/pages/ChatInboxCRM.tsx
--index 22210b7..8ea766c 100644
----- a/src/pages/ChatInboxCRM.tsx
--+++ b/src/pages/ChatInboxCRM.tsx
--@@ -1244,6 +1244,25 @@ export default function ChatInboxCRM() {
--     setActionLoading(false)
--   }
-- 
--+  async function changeConversationQueue(newFila: QueueType) {
--+    if (!selectedConversation) return
--+    setActionLoading(true)
--+    setActionError(null)
--+    try {
--+      const response = await fetch(getApiUrl(`/chat/crm/conversations/${selectedConversation.id}`), {
--+        method: 'PATCH',
--+        headers: { 'Content-Type': 'application/json' },
--+        body: JSON.stringify({ fila: newFila }),
--+      })
--+      const data = await response.json()
--+      if (!data.ok) throw new Error(data.error || 'Falha ao alterar fila')
--+      await loadConversations(false)
--+    } catch (err) {
--+      setActionError(err instanceof Error ? err.message : String(err))
--+    }
--+    setActionLoading(false)
--+  }
--+
--   async function updateConversationStatusById(conversationId: string, status: string) {
--     const shouldExitView = isClosedConversationStatus(status) && conversationId === selectedId
--     const response = await fetch(getApiUrl(`/chat/crm/conversations/${conversationId}`), {
--@@ -2785,7 +2804,15 @@ export default function ChatInboxCRM() {
--                     )}
-- 
--                     <PanelBlock title="Controles do atendimento">
---                      <label className="block text-xs font-semibold uppercase tracking-wide text-slate-500">Etapa do Kanban</label>
--+                      <label className="block text-xs font-semibold uppercase tracking-wide text-slate-500">Fila</label>
--+                      <select value={selectedConversation.fila} onChange={event => void changeConversationQueue(event.target.value as QueueType)} disabled={actionLoading} className="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm outline-none">
--+                        <option value="atendimento">Atendimento</option>
--+                        <option value="renovacao">Renovacao</option>
--+                        <option value="email">Email</option>
--+                        <option value="agendamento">Agendamento</option>
--+                      </select>
--+
--+                      <label className="mt-3 block text-xs font-semibold uppercase tracking-wide text-slate-500">Etapa do Kanban</label>
--                       <select value={normalizeKanbanStatus(selectedConversation.kanban_status)} onChange={event => void updateConversationStatus(event.target.value)} disabled={actionLoading} className="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm outline-none">
--                         {STATUS_OPTIONS.map(column => (
--                           <option key={column.key} value={column.key}>{column.label}</option>
diff --git a/pending-commits/aplicar-todos.ps1 b/pending-commits/aplicar-todos.ps1
index 700f0ea..e053814 100644
--- a/pending-commits/aplicar-todos.ps1
+++ b/pending-commits/aplicar-todos.ps1
@@ -42,7 +42,7 @@ foreach ($Patch in $Patches) {
 
   if ($DryRun) {
     & git apply --stat $Patch.FullName
-    Write-Host "  (dry-run — nao aplicado)"
+    Write-Host "  (dry-run - nao aplicado)"
   } else {
     & git apply $Patch.FullName
     & git add -A

{
  "name": "AVMD - Email Send (SMTP)",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "avmd-email-send",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook-email-send",
      "name": "Email Send Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [420, 300]
    },
    {
      "parameters": {
        "fromEmail": "=naoresponda@certiid.com.br",
        "toEmail": "={{ $json.body?.to || $json.to }}",
        "subject": "={{ $json.body?.subject || $json.subject }}",
        "text": "={{ $json.body?.body || $json.body || '' }}",
        "html": "={{ $json.body?.html || $json.html || $json.body?.payload?.html || $json.payload?.html || $json.body?.body || $json.body || '' }}",
        "options": {}
      },
      "id": "smtp-send",
      "name": "Send Email SMTP",
      "type": "n8n-nodes-base.emailSend",
      "typeVersion": 2.1,
      "position": [700, 300],
      "credentials": {
          "smtp": {
          "id": "jkjdGJa8eyLZvZko",
          "name": "SMTP não responda certiid"
        }
      }
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ { ok: $json?.accepted?.length > 0, sent: $json?.accepted?.length > 0, accepted: $json?.accepted || [], rejected: $json?.rejected || [], response: $json?.response || null, to: $json.to, subject: $json.subject } }}",
        "options": {
          "responseCode": 200
        }
      },
      "id": "ack-send",
      "name": "Ack Email Send",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [980, 300]
    }
  ],
  "connections": {
    "Email Send Webhook": {
      "main": [[{ "node": "Send Email SMTP", "type": "main", "index": 0 }]]
    },
    "Send Email SMTP": {
      "main": [[{ "node": "Ack Email Send", "type": "main", "index": 0 }]]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "active": true,
  "versionId": "1"
}
